{
  "name": "fempp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage"
  },
  "devDependencies": {
    "cypress": "^14.2.1",
    "jest": "^29.7.0",
    "vite": "^6.2.0"
  }
}
